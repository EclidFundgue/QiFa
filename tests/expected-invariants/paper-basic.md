# paper-basic 不变量

- `intake` 完成时：`project.yaml` 通过 schema；`00-intake.md` 存在且写入了 5 项确认结果。
- `source-analysis` 完成时：`source/source-model.json` 通过 schema；论文条目包含 `Eq.1 / Fig.1 / Tab.1 / §2`；全部引用可解析。
- `curriculum` 完成时：学习目标 ≤ 3 条且全部被章节承接；章节数 1–4；`duration_estimate` 与讲稿换算差异 ≤ 1.5 分钟。
- 无 blocker；允许时长偏差等 warning，但必须写入 `review/qa-report.md`。
