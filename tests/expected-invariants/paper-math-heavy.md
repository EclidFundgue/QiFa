# paper-math-heavy 不变量

- 公式页（`kind: formula-steps`）至少覆盖 `Eq.1`，且公式块不计入时长统计。
- `narrative` 完成时：讲稿锚点与章节计划页面一一对应，顺序一致。
- 深度 `deep` 下，每章 `duration_estimate` 的合计与 45 分钟目标的偏差在容差内，或作为 warning 记录。
- 当前夹具为占位（`tiny-paper.tex` 公式量不足以代表 math-heavy）；真实用例应在替换夹具后补上公式覆盖断言。
