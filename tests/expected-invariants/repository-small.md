# repository-small 不变量

- 代码引用只能是 `path:符号`：`main.py:main`、`greet.py:greet` 必须存在。
- `relevance.relation` 为 `not-applicable`（无论文输入）。
- 章节计划里至少一张 `code-walkthrough` 页，且引用真实符号。
- 不设练习与自测；`chapter-plan` 不含练习字段。
